{
  "name": "cyra-discord-bot",
  "version": "1.1.0",
  "description": "Cyra — a context-aware, sarcastic fact-checking Discord bot powered by xAI.",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check config.js && node --check context.js && node --check prompt.js && node --check utils.js && node --check xai.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "discord.js": "^14.22.1"
  }
}
